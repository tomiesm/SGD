# SGD manuscript replication — pinned dependencies.
# Python 3.12.2.
#
# Versions are pinned to the proven environment of the sibling `sgd/`
# curation repo (environment frozen 2026-05-17), which is the environment
# every reported number was computed in.
#
# These exact versions are also reported in the revised manuscript. They are
# the environment in which the revision artifacts were regenerated.

# Core scientific stack
numpy==2.0.2
scipy==1.16.3
pandas==2.2.3
scikit-learn==1.6.1
statsmodels==0.14.6
matplotlib==3.10.8
joblib==1.5.0

# Spatial transcriptomics and single-cell
anndata==0.12.10
scanpy==1.12
scikit-misc==0.5.1       # required by scanpy HVG flavor="seurat_v3" in step 02
dask[array]==2024.8.2    # lazy HD h5ad column loading in step 09
xarray==2026.1.0         # AnnData read_lazy table backend

# File formats
pyarrow==17.0.0          # parquet read/write
openpyxl==3.1.5          # Yakubovsky supplementary-table xlsx reads
Pillow==11.1.0           # raster image input used by figure 1
patsy==1.0.1             # explicit design-matrix dependency for steatosis inference

# Pathway enrichment (step 14)
gseapy==1.1.9

# --- GPU, step 10 only (cell2location is GPU-bound, ~2 h) ---
# Required only by scripts/10_cell2location.py. A CPU-only replicator can
# skip these and run step 10's second pass from the cached parquet.
# cell2location==0.1.5
# scvi-tools==1.4.2
# torch==2.6.0
