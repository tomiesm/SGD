"""sgd — shared computation modules for the SGD manuscript replication.

Importable submodules: config, io, panels, axis, gradient, confounds,
wmatrix, steatosis, plotstyle. Entry-point scripts under ``scripts/``
import from these via ``from sgd.<module> import <name>``.
"""
